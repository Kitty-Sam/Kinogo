import { width } from '~constants/dimensions';

export const CARD_LEN = width * 0.6;
export const SPACING = width * 0.04;
export const SIDE_CARD_LEN = (width * 0.04) / 2;
export const snapToInterval = CARD_LEN + SPACING * 1.5;
